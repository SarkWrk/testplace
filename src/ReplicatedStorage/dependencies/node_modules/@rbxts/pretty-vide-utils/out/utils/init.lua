-- Compiled with roblox-ts v2.3.0-dev-ff69514
local function lockSource(source)
	return function()
		return source()
	end
end
local function getDerivable(value)
	local _value = value
	if type(_value) == "function" then
		return value()
	else
		return value
	end
end
local function listenSources(...)
	local sources = { ... }
	-- ▼ ReadonlyArray.forEach ▼
	local _callback = function(source)
		return source()
	end
	for _k, _v in sources do
		_callback(_v, _k - 1, sources)
	end
	-- ▲ ReadonlyArray.forEach ▲
end
return {
	lockSource = lockSource,
	getDerivable = getDerivable,
	listenSources = listenSources,
}
