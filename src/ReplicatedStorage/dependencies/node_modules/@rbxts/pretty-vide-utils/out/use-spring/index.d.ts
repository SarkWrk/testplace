import { MotionGoal, SpringOptions } from "@rbxts/ripple";
import { Derivable } from "@rbxts/vide";
import { LockedSource } from "../utils";
export declare function useSpring(goal: Derivable<number>, options?: SpringOptions): LockedSource<number>;
export declare function useSpring<T extends MotionGoal>(goal: Derivable<T>, options?: SpringOptions): LockedSource<T>;
