-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local effect = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).effect
local _utils = TS.import(script, script.Parent, "utils")
local getDerivable = _utils.getDerivable
local lockSource = _utils.lockSource
local useMotion = TS.import(script, script.Parent, "use-motion").useMotion
local function useSpring(goal, options)
	local value, motion = useMotion(getDerivable(goal))
	effect(function()
		local _goal = getDerivable(goal)
		motion:spring(_goal, options)
	end)
	return lockSource(value)
end
return {
	useSpring = useSpring,
}
