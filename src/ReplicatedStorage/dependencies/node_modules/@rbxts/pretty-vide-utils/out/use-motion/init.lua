-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local createMotion = TS.import(script, TS.getModule(script, "@rbxts", "ripple").src).createMotion
local lockSource = TS.import(script, script.Parent, "utils").lockSource
local source = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).source
local useEventListener = TS.import(script, script.Parent, "use-event-listener").useEventListener
local RunService = TS.import(script, TS.getModule(script, "@rbxts", "services")).RunService
local function useMotion(initialValue)
	local motion = createMotion(initialValue)
	local motionValue = source(initialValue)
	useEventListener(RunService.Heartbeat, function(delta)
		local value = motion:step(delta)
		if value ~= motionValue() then
			motionValue(value)
		end
	end)
	return lockSource(motionValue), motion
end
return {
	useMotion = useMotion,
}
