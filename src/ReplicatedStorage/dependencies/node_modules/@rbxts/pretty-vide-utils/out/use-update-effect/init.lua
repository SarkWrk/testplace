-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local effect = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).effect
local function useUpdateEffect(callback)
	local isMounted = false
	effect(function()
		if isMounted then
			callback()
		else
			isMounted = true
		end
	end)
end
return {
	useUpdateEffect = useUpdateEffect,
}
