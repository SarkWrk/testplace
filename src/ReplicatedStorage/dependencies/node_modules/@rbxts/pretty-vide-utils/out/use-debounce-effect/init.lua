-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local effect = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).effect
local useDebounceCallback = TS.import(script, script.Parent, "use-debounce-callback").useDebounceCallback
local function useDebounceEffect(debounced, callback, options)
	local _binding = useDebounceCallback(debounced, options)
	local run = _binding.run
	effect(function()
		callback()
		run()
	end)
end
return {
	useDebounceEffect = useDebounceEffect,
}
