-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local source = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).source
local useDebounceCallback = TS.import(script, script.Parent, "use-debounce-callback").useDebounceCallback
local function useDebounceSource(init, options)
	local debounceSource = source(init)
	local _binding = useDebounceCallback(debounceSource, options)
	local run = _binding.run
	local proxy = function(value)
		if value == nil then
			return debounceSource()
		end
		run(value)
		return value
	end
	return proxy
end
return {
	useDebounceSource = useDebounceSource,
}
