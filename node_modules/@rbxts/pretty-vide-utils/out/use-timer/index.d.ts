/**
 * Creates a timer that can be used to track a value over time.
 * @param initialValue The initial value of the timer.
 * @returns A timer object.
 */
export declare function useTimer(initial?: number): {
    timer: () => number;
    start: () => void;
    stop: () => void;
    reset: () => void;
    set: (value: number) => void;
};
