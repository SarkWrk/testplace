/**
 * Returns the lifetime of the component in seconds. Updates every frame on
 * the Heartbeat event.
 *
 * @returns A binding of the component's lifetime.
 */
export declare function useLifetime<T>(): import("@rbxts/vide").Source<number>;
