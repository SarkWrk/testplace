-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local _vide = TS.import(script, TS.getModule(script, "@rbxts", "vide").src)
local effect = _vide.effect
local source = _vide.source
local useAsync = TS.import(script, script.Parent).useAsync
local Players = TS.import(script, TS.getModule(script, "@rbxts", "services")).Players
local GetUserId = TS.async(function(name)
	return Players:GetUserIdFromNameAsync(name)
end)
local function Component()
	local username = source("PepeElToro41")
	local userid = useAsync(function()
		return GetUserId(username())
	end)
	effect(function()
		return print((userid()))
	end)
end
return nil
