-- Compiled with roblox-ts v2.3.0-dev-ff69514
local TS = _G[script]
local effect = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).effect
local useThrottleCallback = TS.import(script, script.Parent, "use-throttle-callback").useThrottleCallback
local function useThrottleEffect(callback, debounced, options)
	local _binding = useThrottleCallback(debounced, options)
	local run = _binding.run
	effect(function()
		callback()
		run()
	end)
end
return {
	useThrottleEffect = useThrottleEffect,
}
